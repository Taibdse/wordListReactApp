import React, { Component } from 'react';

class AddCompo extends Component {
    render() {
        return (
            <div className = "">
                <button className = "btn btn-primary" onClick = {}>Add more</button>
            </div>
        );
    }
}

export default AddCompo;